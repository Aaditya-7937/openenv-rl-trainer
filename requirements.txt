# ── PyTorch ──────────────────────────────────────────────────────────────────
# DO NOT pin torch here.
# The HuggingFace Unsloth Space image pre-installs torch 2.11.0+cu130 (CUDA-enabled).
# Pinning a lower version (e.g. 2.6.0) DOWNGRADES torch and breaks torchao,
# which requires torch.utils._pytree.register_constant (added in torch 2.7.0).
# Let the container own its torch version.

# ── Core training stack ──────────────────────────────────────────────────────
transformers>=4.40.0
accelerate>=0.26.0       # required by TRL GRPOTrainer
peft>=0.10.0
bitsandbytes>=0.43.0     # required for NF4 4-bit quant on CUDA 12
trl>=0.9.0
unsloth
datasets

# ── App + environment deps ───────────────────────────────────────────────────
httpx
matplotlib
python-dotenv
fastapi
uvicorn
