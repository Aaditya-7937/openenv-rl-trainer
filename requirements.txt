# ── CUDA-enabled PyTorch (CPU-only torch from PyPI will break Unsloth+TRL) ──
# Space runs CUDA 12.3 — use cu121 (compiled for CUDA 12.1, runs on 12.3 drivers).
# Rule: wheel CUDA version must be <= driver CUDA version.
--extra-index-url https://download.pytorch.org/whl/cu121
torch==2.6.0
torchvision
torchaudio

# ── Core training stack ──────────────────────────────────────────────────────
transformers>=4.40.0
accelerate>=0.26.0       # required by TRL GRPOTrainer
peft>=0.10.0
bitsandbytes>=0.43.0     # required for NF4 4-bit quant on CUDA 12
trl>=0.9.0
unsloth
datasets

# ── App + environment deps ───────────────────────────────────────────────────
httpx
matplotlib
python-dotenv
fastapi
uvicorn
