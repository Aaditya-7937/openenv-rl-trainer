# ── PyTorch ──────────────────────────────────────────────────────────────────
# A100 Space has CUDA 12.3 driver. Two hard constraints:
#   1. Wheel CUDA version must be <= driver version → cu121 (12.1 <= 12.3) ✓
#      Unpinned unsloth pulls torch 2.11.0+cu130 which needs CUDA 13.0 → fails.
#   2. torch >= 2.7.0 needed for torch.utils._pytree.register_constant (torchao) ✓
# torch 2.7.0+cu121 satisfies both.
--extra-index-url https://download.pytorch.org/whl/cu121
torch==2.7.0

# ── Core training stack ──────────────────────────────────────────────────────
transformers>=4.40.0
accelerate>=0.26.0       # required by TRL GRPOTrainer
peft>=0.10.0
bitsandbytes>=0.43.0     # required for NF4 4-bit quant on CUDA 12
trl>=0.9.0
unsloth
datasets

# ── App + environment deps ───────────────────────────────────────────────────
httpx
matplotlib
python-dotenv
fastapi
uvicorn
